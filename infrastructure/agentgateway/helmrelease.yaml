apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: agentgateway
  namespace: agentic
spec:
  interval: 5m
  chart:
    spec:
      chart: agentgateway
      sourceRef:
        kind: HelmRepository
        name: agentgateway
        namespace: flux-system